#from EmotionNeuralInterface.data.subject_data import Subjects
"""
deprecated
def experiment_to_subject(experiments):
  #Agruoa los datos que estan por experimento a sujeto.
  #
  #Args:
  #    experiments (obj): Instancia de la clase Experiment
  #
  #Returns:
  #    Subjects: Instancia de la clase Subjects donde se almacenan los sujetos
   
  subjects = Subjects()
  for exp in experiments.values():
    for data in exp:
      subjects.append(data)
  return subjects

"""